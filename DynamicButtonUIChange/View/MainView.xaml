<Window x:Class="DynamicButtonUIChange.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
        xmlns:controls="clr-namespace:DynamicButtonUIChange.ButtonCommand"
        xmlns:UIView="clr-namespace:DynamicButtonUIChange.ViewModel"
    
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- ViewをDataTemplateで定義 -->
        <DataTemplate DataType="{x:Type UIView:View1}">
            <StackPanel>
                <UIView:View1/>
                <UIView:View2/>
                <UIView:View3/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch"  />

        <StackPanel VerticalAlignment="Center">
            <Button x:Name="UIChange1" Content="UI1" Margin="20"
             Command="{x:Static controls:ButtonCommand.UIView1}"/>
            <Button x:Name="UIChange2" Content="UI2"
            Margin="20"/>
            <Button x:Name="UIChange3" Content="UI3"
            Margin="20"/>
        </StackPanel>
        <ContentControl Grid.Column="2" Content="{Binding CurrentView}" />
    </Grid>
</Window>
