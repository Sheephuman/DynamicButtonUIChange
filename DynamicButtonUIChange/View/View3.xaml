<UserControl x:Class="DynamicUIChange.Views.View3"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DynamicUIChange.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <DockPanel DockPanel.Dock="Top">
            <StackPanel Orientation="Vertical">
           
                    <Button x:Name="ButtonGo"
                    
                    Command="{Binding UiView3Command}"
            VerticalAlignment="Top"
            Height="30"
              DockPanel.Dock="Right"
             FontSize="20"
              Content="Go"
              
    />
            <TextBox  Height="30"
            Text="{Binding URLstrings}"
            VerticalAlignment="Top" Name="addressBar" 
            FontSize="22"/>
                <wv2:WebView2 
                Height="400"
               Name="webView"
               Source= "{Binding BrowserUri}"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               Margin="10"
               
               />
            </StackPanel>
            </DockPanel>
           
    </Grid>
</UserControl>
