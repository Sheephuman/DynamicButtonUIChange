<UserControl x:Class="DynamicUIChange_Prism.Views.View2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              
             xmlns:local="clr-namespace:DynamicUIChange_Prism.ViewModels" 
             mc:Ignorable="d" 
          xmlns:prism="http://prismlibrary.com/"
         prism:ViewModelLocator.AutoWireViewModel="True"

             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GridSplitter Grid.Row="1" VerticalAlignment="Stretch"  />
       
            
        <DataGrid x:Name="dataGrid1" ItemsSource="{Binding People}" AutoGenerateColumns="False"
        ScrollViewer.VerticalScrollBarVisibility="Auto" 
        HeadersVisibility="Column" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="年齢" Binding="{Binding Age}" Width="*" />
            </DataGrid.Columns>
                
            </DataGrid>
            
        <Button Grid.Row="2" Content="test" Width="200" Height="50"
Command="{Binding UIViewCommand2}"/>    
       
     
        
    </Grid>
</UserControl>
