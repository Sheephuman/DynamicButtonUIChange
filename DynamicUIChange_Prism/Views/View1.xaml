<UserControl x:Class="DynamicUIChange_Prism.Views.View1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DynamicUIChange_Prism.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Tab 1">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="This is View 1" FontSize="24" Margin="10"/>
                    <Button Content="Click Me!" Command="{Binding Button1Command}" Margin="10"/>
                    <Canvas>
                        <Image Source="{Binding BackImage}" Width="300" Height="200" />
                    </Canvas>
                </StackPanel>
            </TabItem>
            <TabItem Header="Tab 2">
                <StackPanel>
                    <TextBlock Text="This is View 1" FontSize="24" Margin="10"/>
                    <Button Content="Click Me!" Command="{Binding Button2Command}" Margin="10"/>

                    <Image x:Name="imgView2" 
                    Source="{Binding BackImage2}" 
                           Width="300" 

                           Height="200"
                           Margin="10"/>
                </StackPanel>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
